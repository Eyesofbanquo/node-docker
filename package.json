{
  "name": "node-docker",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "test": "mocha -r ts-node/register *.ts --exit",
    "dev": "nodemon -x ts-node ./server.ts",
    "start": "ts-node ./server.ts",
    "docker:open:test": "docker build -f ./Dockerfile.ci -t avon-test . && docker run --name avon_test -it --entrypoint=/bin/bash avon-test -i",
    "docker:test": "docker-compose -f docker-compose.ci.yml up --exit-code-from web",
    "tsc": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.9",
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.6",
    "ts-node": "^9.0.0",
    "tsc-node": "0.0.3",
    "typescript": "^4.0.5"
  },
  "devDependencies": {
    "@types/chai": "^4.2.14",
    "@types/mocha": "^8.0.4",
    "chai": "^4.2.0",
    "mocha": "^8.2.1"
  }
}
